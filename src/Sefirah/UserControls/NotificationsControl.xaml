<UserControl
    x:Class="Sefirah.UserControls.NotificationsControl"
    x:Name="NotificationRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:Sefirah.Converters"
    xmlns:wctconverters="using:CommunityToolkit.WinUI.Converters"
    xmlns:helpers="using:Sefirah.Helpers"
    xmlns:models="using:Sefirah.Data.Models">

    <UserControl.Resources>
        <ResourceDictionary>
            <converters:EmptyObjectToVisibilityConverter x:Key="EmptyObjectToVisibilityConverter" />
            <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <wctconverters:BoolToObjectConverter
                x:Key="BoolToPinTextConverter"
                FalseValue="{helpers:ResourceString Name=Pin}"
                TrueValue="{helpers:ResourceString Name=Unpin}" />
            <wctconverters:BoolToObjectConverter
                x:Key="BoolToPinGlyphConverter"
                FalseValue="&#xE718;"
                TrueValue="&#xE77A;" />
            <wctconverters:BoolNegationConverter x:Key="BoolNegationConverter" />
            <converters:UnixTimestampConverter x:Key="UnixTimestampConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock
                Grid.Column="0"
                FontSize="20"
                FontWeight="SemiBold"
                Text="{helpers:ResourceString Name=Notifications}" />
            <Button
                Grid.Column="1"
                Background="Transparent"
                BorderBrush="Transparent"
                Command="{x:Bind ViewModel.ClearAllNotificationsCommand}"
                Content="{helpers:ResourceString Name=NotificationsClearButton}" />
        </Grid>

        <ScrollViewer
            Grid.Row="1"
            Margin="0,8,-14,0"
            Padding="0,0,14,0">
            <ItemsRepeater ItemsSource="{x:Bind ViewModel.Notifications, Mode=OneWay}" VerticalCacheLength="6">
                <ItemsRepeater.Layout>
                    <StackLayout Orientation="Vertical" />
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="models:Notification">
                        <Border
                            Padding="8"
                            Margin="0,0,0,8"
                            Background="{ThemeResource CardBackgroundFillColorDefault}"
                            BorderBrush="{ThemeResource CardStrokeColorDefault}"
                            BorderThickness="1"
                            CornerRadius="8"
                            PointerEntered="OnPointerEntered"
                            PointerExited="OnPointerExited">
                            <Grid RowSpacing="12" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Image
                                    Grid.Row="0"
                                    Grid.Column="0"
                                    Width="48"
                                    Height="48"
                                    VerticalAlignment="Top"
                                    Source="{x:Bind helpers:ImageHelper.ToDisplayIcon(LargeIcon, DeviceId, AppPackage), Mode=OneWay}"
                                    Visibility="{x:Bind HasDisplayIcon, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}" />
                                <StackPanel Grid.Row="0" Grid.Column="1" Spacing="4">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock
                                            Grid.Column="0"
                                            Foreground="{ThemeResource TextFillColorSecondary}"
                                            FontWeight="Medium"
                                            Text="{x:Bind AppName}" />
                                        <StackPanel
                                            Grid.Column="1"
                                            HorizontalAlignment="Right"
                                            Orientation="Horizontal"
                                            Spacing="8">
                                            <Button
                                                x:Name="MoreButton"
                                                Width="20"
                                                Height="20"
                                                Padding="0"
                                                AutomationProperties.Name="{helpers:ResourceString Name=ContextMenu}"
                                                Background="Transparent"
                                                BorderBrush="Transparent"
                                                Opacity="0">
                                                <SymbolIcon Symbol="More" />
                                                <Button.Flyout>
                                                    <MenuFlyout Closed="MoreButtonFlyoutClosed" Placement="BottomEdgeAlignedLeft">
                                                        <MenuFlyoutItem
                                                            Click="OpenAppClick"
                                                            DataContext="{x:Bind}"
                                                            Text="{helpers:ResourceString Name=OpenApp}">
                                                            <MenuFlyoutItem.Icon>
                                                                <FontIcon Glyph="&#xED35;" />
                                                            </MenuFlyoutItem.Icon>
                                                        </MenuFlyoutItem>
                                                        <MenuFlyoutItem
                                                            Click="UpdateNotificationFilterClick"
                                                            Tag="{x:Bind AppPackage}"
                                                            Text="{x:Bind FlyoutFilterString}">
                                                            <MenuFlyoutItem.Icon>
                                                                <FontIcon Glyph="&#xECE4;" />
                                                            </MenuFlyoutItem.Icon>
                                                        </MenuFlyoutItem>
                                                        <MenuFlyoutItem
                                                            Click="ToggleNotificationPinClick"
                                                            DataContext="{x:Bind}"
                                                            Text="{x:Bind Pinned, Converter={StaticResource BoolToPinTextConverter}}">
                                                            <MenuFlyoutItem.Icon>
                                                                <FontIcon Glyph="{x:Bind Pinned, Converter={StaticResource BoolToPinGlyphConverter}}" />
                                                            </MenuFlyoutItem.Icon>
                                                        </MenuFlyoutItem>
                                                    </MenuFlyout>
                                                </Button.Flyout>
                                            </Button>
                                            <Button
                                                x:Name="CloseButton"
                                                Width="20"
                                                Height="20"
                                                Padding="0"
                                                AutomationProperties.Name="{helpers:ResourceString Name=Dismiss}"
                                                Command="{Binding ViewModel.RemoveNotificationCommand, ElementName=NotificationRoot}"
                                                CommandParameter="{x:Bind}"
                                                Opacity="0"
                                                Style="{StaticResource AlternateCloseButtonStyle}" />
                                            <SymbolIcon
                                                x:Name="PinIcon"
                                                RenderTransformOrigin="0.5,0.5"
                                                Symbol="Pin"
                                                Tag="{x:Bind Pinned}"
                                                Visibility="{x:Bind Pinned, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <SymbolIcon.RenderTransform>
                                                    <ScaleTransform ScaleX="0.7" ScaleY="0.7" />
                                                </SymbolIcon.RenderTransform>
                                            </SymbolIcon>
                                            <TextBlock
                                                x:Name="TimeStampTextBlock"
                                                Foreground="{ThemeResource TextFillColorTertiary}"
                                                HorizontalTextAlignment="End"
                                                Text="{x:Bind TimestampMillis, Converter={StaticResource UnixTimestampConverter}}"
                                                Visibility="Visible" />
                                        </StackPanel>
                                    </Grid>
                                    <TextBlock
                                        x:Name="TitleTextBlock"
                                        x:Load="{x:Bind ShouldShowTitle}"
                                        FontWeight="SemiBold"
                                        Text="{x:Bind Title}"
                                        IsTextSelectionEnabled="True"
                                        TextWrapping="Wrap" />
                                    <TextBlock
                                        x:Name="FallbackTextBlock"
                                        x:Load="{x:Bind HasGroupedMessages, Converter={StaticResource BoolNegationConverter}}"
                                        Foreground="{ThemeResource TextFillColorTertiary}"
                                        IsTextSelectionEnabled="True"
                                        Text="{x:Bind Text}"
                                        TextWrapping="Wrap" />
                                    <ItemsRepeater
                                        x:Name="GroupedMessagesRepeater"
                                        HorizontalAlignment="Stretch"
                                        x:Load="{x:Bind HasGroupedMessages}"
                                        ItemsSource="{x:Bind GroupedMessages}">
                                        <ItemsRepeater.Layout>
                                            <StackLayout Orientation="Vertical" Spacing="8" />
                                        </ItemsRepeater.Layout>
                                        <ItemsRepeater.ItemTemplate>
                                            <DataTemplate x:DataType="models:NotificationGroup">
                                                <StackPanel>
                                                    <TextBlock FontWeight="SemiBold" Text="{x:Bind Sender}" />
                                                    <ItemsRepeater ItemsSource="{x:Bind Messages}">
                                                        <ItemsRepeater.Layout>
                                                            <StackLayout Orientation="Vertical" Spacing="2" />
                                                        </ItemsRepeater.Layout>
                                                        <ItemsRepeater.ItemTemplate>
                                                            <DataTemplate x:DataType="x:String">
                                                                <TextBlock
                                                                    Foreground="{ThemeResource TextFillColorTertiary}"
                                                                    IsTextSelectionEnabled="True"
                                                                    Text="{x:Bind}"
                                                                    TextWrapping="Wrap" />
                                                            </DataTemplate>
                                                        </ItemsRepeater.ItemTemplate>
                                                    </ItemsRepeater>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsRepeater.ItemTemplate>
                                    </ItemsRepeater>
                                </StackPanel>
                                <StackPanel
                                    x:Name="ActionsPanel"
                                    Grid.Row="1"
                                    Grid.Column="0"
                                    Grid.ColumnSpan="2"
                                    Spacing="8">
                                    <Grid
                                        x:Name="ReplyActionGrid"
                                        ColumnSpacing="4"
                                        Visibility="{x:Bind ReplyResultKey, Converter={StaticResource EmptyObjectToVisibilityConverter}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <TextBox
                                            x:Name="ReplyTextBox"
                                            Grid.Column="0"
                                            KeyDown="ReplyTextBox_KeyDown"
                                            PlaceholderText="{helpers:ResourceString Name=Reply}"
                                            Tag="{x:Bind}" />
                                        <Button
                                            x:Name="SendButton"
                                            Grid.Column="1"
                                            Background="Transparent"
                                            BorderBrush="Transparent"
                                            AutomationProperties.Name="{helpers:ResourceString Name=SendNotificationReply}"
                                            Click="SendButton_Click"
                                            Tag="{x:Bind}">
                                            <FontIcon Glyph="&#xE724;" />
                                        </Button>
                                    </Grid>
                                    <ItemsRepeater ItemsSource="{x:Bind Actions}">
                                        <ItemsRepeater.Layout>
                                            <StackLayout Orientation="Horizontal" Spacing="4" />
                                        </ItemsRepeater.Layout>
                                        <ItemsRepeater.ItemTemplate>
                                            <DataTemplate x:DataType="models:NotificationAction">
                                                <Button
                                                    Background="Transparent"
                                                    BorderBrush="Transparent"
                                                    Command="{Binding ViewModel.HandleNotificationActionCommand, ElementName=NotificationRoot}"
                                                    CommandParameter="{x:Bind}"
                                                    Content="{x:Bind Label}" />
                                            </DataTemplate>
                                        </ItemsRepeater.ItemTemplate>
                                    </ItemsRepeater>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</UserControl>
