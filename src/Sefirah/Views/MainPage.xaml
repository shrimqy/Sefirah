<Page
    x:Class="Sefirah.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:Sefirah.Converters"
    xmlns:wctconverters="using:CommunityToolkit.WinUI.Converters"
    xmlns:wuc="using:CommunityToolkit.WinUI.Controls"
    xmlns:helpers="using:Sefirah.Helpers"
    xmlns:local="using:Sefirah.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Sefirah.Data.Models"
    xmlns:not_win="http://uno.ui/not_win"
    xmlns:userControls="using:Sefirah.UserControls"
    xmlns:utu="using:Uno.Toolkit.UI"
    xmlns:win="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    Background="{ThemeResource App.PageBackgroundBrush}"
    mc:Ignorable="not_win"
    x:Name="PageRoot">

    <Page.Resources>
        <wctconverters:BoolToObjectConverter x:Key="BoolToConnectionButtonTextConverter"
            FalseValue="{helpers:ResourceString Name=Disconnected/Text}"
            TrueValue="{helpers:ResourceString Name=Connected/Text}" />
        
        <wctconverters:BoolToObjectConverter x:Key="BoolToPinTextConverter"
            FalseValue="{helpers:ResourceString Name=Pin}"
            TrueValue="{helpers:ResourceString Name=Unpin}" />
        
        <wctconverters:BoolToObjectConverter x:Key="BoolToDndButtonStyleConverter"
            FalseValue="{StaticResource DefaultButtonStyle}"
            TrueValue="{StaticResource AccentButtonStyle}" />

        <converters:CountToBooleanConverter x:Key="CountToBooleanConverter" />
        <converters:CountToBooleanConverter x:Key="InverseCountToBooleanConverter" Inverse="True" />

        <wctconverters:EmptyObjectToObjectConverter x:Key="EmptyObjectToOpacityConverter" EmptyValue="1.0" NotEmptyValue="0.0" />
        <wctconverters:BoolToObjectConverter x:Key="BoolToPinGlyphConverter" FalseValue="&#xE718;" TrueValue="&#xE77A;" />
        <wctconverters:BoolToObjectConverter x:Key="BoolToPlayPauseGlyphConverter" FalseValue="&#xE768;" TrueValue="&#xE769;" />
        <wctconverters:BoolNegationConverter x:Key="BoolNegationConverter" />

        <converters:DateTimeConverter x:Key="DateTimeConverter" />
        <converters:RingerModeToIconConverter x:Key="RingerModeToIconConverter" />
        <converters:BatteryStatusToIconConverter x:Key="BatteryStatusToIconConverter" />
        <converters:MillisecondsToTimeConverter x:Key="MillisecondsToTimeConverter" />
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:BooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" Inverse="True" />
        <converters:EmptyObjectToVisibilityConverter x:Key="EmptyObjectToVisibilityConverter" />
        <converters:EmptyObjectToVisibilityConverter x:Key="InverseEmptyObjectToVisibilityConverter" Inverse="True" />

        <!-- Phone Frame Overlay Animations -->
        <Storyboard x:Key="FadeInAnimation" x:Name="FadeInStoryboard">
            <DoubleAnimation 
                Storyboard.TargetName="PhoneFrameOverlay" 
                Storyboard.TargetProperty="Opacity" 
                From="0" 
                To="1" 
                Duration="0:0:0.200" />
        </Storyboard>
        
        <Storyboard x:Key="FadeOutAnimation" x:Name="FadeOutStoryboard">
            <DoubleAnimation 
                Storyboard.TargetName="PhoneFrameOverlay" 
                Storyboard.TargetProperty="Opacity" 
                From="1" 
                To="0" 
                Duration="0:0:0.200" />
        </Storyboard>
    </Page.Resources>

    <Grid
        AllowDrop="True"
        DragOver="Grid_DragOver"
        Drop="Page_Drop">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <win:Grid Grid.Row="0">
            <userControls:TitleBar />
        </win:Grid>
        <not_win:Grid Grid.Row="0" Height="8" />
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!--  Left Panel: Device Info and Notifications  -->
            <Grid Grid.Column="0" Margin="18,0" Padding="0" RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!--  Connected Device View  -->
                <Grid
                    Grid.Row="0"
                    ColumnSpacing="18"
                    Visibility="{x:Bind ViewModel.Device, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid
                        x:Name="PhoneFrameGrid"
                        Grid.Column="0"
                        Width="54"
                        Height="100"
                        VerticalAlignment="Center"
                        CornerRadius="8"
                        PointerEntered="PhoneFrame_PointerEntered"
                        PointerExited="PhoneFrame_PointerExited"
                        PointerWheelChanged="PhoneFrame_PointerWheelChanged">
                        <!--  Phone frame border  -->
                        <Rectangle
                            Opacity="0.5"
                            RadiusX="4"
                            RadiusY="4"
                            Stroke="Black"
                            StrokeThickness="2">
                            <Rectangle.Fill>
                                <SolidColorBrush Opacity="0.1" Color="{ThemeResource SystemBaseLowColor}" />
                            </Rectangle.Fill>
                        </Rectangle>
                        <!--  Inner screen border  -->
                        <Rectangle
                            Margin="2"
                            RadiusX="4"
                            RadiusY="4"
                            Stroke="Black"
                            StrokeThickness="1" />
                        <!--  Wallpaper  -->
                        <Border Margin="3" CornerRadius="4">
                            <Border.Background>
                                <LinearGradientBrush Opacity="{x:Bind ViewModel.Device.Wallpaper, Converter={StaticResource EmptyObjectToOpacityConverter}, Mode=OneWay}" StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Offset="0" Color="#FF0070FF" />
                                    <GradientStop Offset="1" Color="#FF2E90FF" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Image
                                x:Name="WallpaperImage"
                                Source="{x:Bind ViewModel.Device.Wallpaper, Mode=OneWay}"
                                Stretch="UniformToFill"
                                Visibility="{x:Bind ViewModel.Device.Wallpaper, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}" />
                        </Border>

                        <!--  Overlay that appears on hover  -->
                        <Grid
                            x:Name="PhoneFrameOverlay"
                            Background="#80000000"
                            Opacity="0"
                            utu:CommandExtensions.Command="{x:Bind ViewModel.StartScrcpyCommand}"
                            Visibility="Collapsed">
                            <FontIcon
                                Foreground="White"
                                Glyph="&#xEC15;"
                                Visibility="{x:Bind ViewModel.LoadingScrcpy, Mode=OneWay, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
                            <ToolTipService.ToolTip>
                                <ToolTip Content="Screen Mirror" />
                            </ToolTipService.ToolTip>
                        </Grid>

                        <!--  Loading indicator  -->
                        <Grid Visibility="{x:Bind ViewModel.LoadingScrcpy, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <ProgressRing
                                Width="24"
                                Height="24"
                                Foreground="White"
                                IsActive="{x:Bind ViewModel.LoadingScrcpy, Mode=OneWay}" />
                        </Grid>
                    </Grid>

                    <!--  Device Info  -->
                    <StackPanel
                        Grid.Column="1"
                        VerticalAlignment="Bottom"
                        Spacing="4">

                        <TextBlock
                            FontWeight="SemiBold"
                            Text="{x:Bind ViewModel.Device.Name, Mode=OneWay}"
                            Visibility="{x:Bind ViewModel.Device.Name, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}" />

                        <StackPanel
                            Orientation="Horizontal"
                            Spacing="4"
                            Visibility="{x:Bind ViewModel.Device.IsConnected, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}">
                            <!--  Audio Mode Button  -->
                            <Button
                                Grid.Column="2"
                                Padding="4"
                                Background="Transparent"
                                BorderThickness="0">
                                <FontIcon Glyph="{x:Bind ViewModel.Device.RingerMode, Converter={StaticResource RingerModeToIconConverter}, Mode=OneWay}" />
                                <Button.Flyout>
                                    <Flyout Placement="Bottom">
                                        <Flyout.FlyoutPresenterStyle>
                                            <Style BasedOn="{StaticResource DefaultFlyoutPresenterStyle}" TargetType="FlyoutPresenter">
                                                <Setter Property="Margin" Value="4,0" />
                                                <Setter Property="MinWidth" Value="280" />
                                            </Style>
                                        </Flyout.FlyoutPresenterStyle>
                                        <StackPanel Spacing="12">
                                            <!--  Volume Sliders  -->
                                            <ItemsRepeater ItemsSource="{x:Bind ViewModel.Device.Streams, Mode=OneWay}" HorizontalAlignment="Stretch">
                                                <ItemsRepeater.Layout>
                                                    <UniformGridLayout
                                                        ItemsStretch="Fill"
                                                        MinItemWidth="64"
                                                        Orientation="Horizontal" />
                                                </ItemsRepeater.Layout>
                                                <ItemsRepeater.ItemTemplate>
                                                    <DataTemplate x:DataType="models:AudioStream">
                                                        <StackPanel Spacing="8" HorizontalAlignment="Stretch">
                                                            <FontIcon Glyph="{x:Bind IconGlyph}" />
                                                            <Slider
                                                                HorizontalAlignment="Center"
                                                                Orientation="Vertical"
                                                                Height="120"
                                                                Minimum="0"
                                                                Maximum="100"
                                                                Value="{x:Bind Level, Mode=TwoWay}"
                                                                PointerCaptureLost="AudioSlider_PointerCaptureLost"
                                                                Tag="{x:Bind}" />
                                                            <TextBlock
                                                                Text="{x:Bind Label}"
                                                                FontSize="12"
                                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                TextWrapping="Wrap"
                                                                HorizontalAlignment="Stretch"
                                                                TextAlignment="Center" />
                                                        </StackPanel>
                                                    </DataTemplate>
                                                </ItemsRepeater.ItemTemplate>
                                            </ItemsRepeater>

                                            <!--  Ringer Mode Selector  -->
                                            <wuc:Segmented HorizontalAlignment="Stretch" SelectedIndex="{x:Bind ViewModel.Device.RingerMode, Mode=OneWay}" SelectionChanged="RingerModeSegmented_SelectionChanged">
                                                <wuc:SegmentedItem Content="{helpers:ResourceString Name=AudioSilent}">
                                                    <wuc:SegmentedItem.Icon>
                                                        <FontIcon Glyph="&#xE7ED;" />
                                                    </wuc:SegmentedItem.Icon>
                                                </wuc:SegmentedItem>
                                                <wuc:SegmentedItem Content="{helpers:ResourceString Name=AudioVibrate}">
                                                    <wuc:SegmentedItem.Icon>
                                                        <FontIcon Glyph="&#xE877;" />
                                                    </wuc:SegmentedItem.Icon>
                                                </wuc:SegmentedItem>
                                                <wuc:SegmentedItem Content="{helpers:ResourceString Name=AudioRing}">
                                                    <wuc:SegmentedItem.Icon>
                                                        <FontIcon Glyph="&#xEA8F;" />
                                                    </wuc:SegmentedItem.Icon>
                                                </wuc:SegmentedItem>
                                            </wuc:Segmented>
                                        </StackPanel>
                                    </Flyout>
                                </Button.Flyout>
                            </Button>

                            <!--  Battery status  -->
                            <StackPanel
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Spacing="1"
                                Visibility="{x:Bind ViewModel.Device.BatteryStatus, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}">
                                <FontIcon Margin="0,0,4,0" Glyph="{x:Bind ViewModel.Device.BatteryStatus, Converter={StaticResource BatteryStatusToIconConverter}, Mode=OneWay}" />
                                <TextBlock FontWeight="SemiBold" Text="{x:Bind ViewModel.Device.BatteryStatus.BatteryLevel, Mode=OneWay}" />
                                <TextBlock FontWeight="SemiBold" Text="%" />
                            </StackPanel>

                        </StackPanel>

                        <!--  Connection Status Card  -->
                        <Button
                            HorizontalAlignment="Left"
                            Command="{x:Bind ViewModel.ToggleConnectionCommand}"
                            Content="{x:Bind ViewModel.Device.ConnectionStatusText, Mode=OneWay}"
                            Visibility="{x:Bind ViewModel.Device.Name, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}" />
                    </StackPanel>
                </Grid>

                <Grid
                    Grid.Row="1"
                    ColumnSpacing="8"
                    Visibility="{x:Bind ViewModel.Device, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!--  DND Toggle  -->
                    <Button
                        Grid.Column="0"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        Padding="8,4"
                        IsEnabled="{x:Bind ViewModel.Device.IsConnected, Mode=OneWay}"
                        Style="{x:Bind ViewModel.Device.DndEnabled, Converter={StaticResource BoolToDndButtonStyleConverter}, Mode=OneWay}"
                        Command="{x:Bind ViewModel.ToggleDndCommand}">
                        <FontIcon Glyph="&#xECE4;" />
                    </Button>

                    <Button
                        Grid.Column="1"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        Padding="8,4"
                        IsEnabled="{x:Bind ViewModel.Device.IsConnected, Mode=OneWay}"
                        Click="SendFilesButton_Click">
                        <FontIcon Glyph="&#xE72D;" />
                    </Button>

                    <!--  Device Settings  -->
                    <Button
                        Grid.Column="2"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        Padding="8,4"
                        Command="{x:Bind ViewModel.OpenDeviceSettingsCommand}">
                        <FontIcon Glyph="&#xE713;" />
                    </Button>
                </Grid>

                <StackPanel
                    Grid.Row="2"
                    Background="Transparent"
                    Visibility="{x:Bind ViewModel.Device, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}">

                    <Rectangle Height="1" Fill="{ThemeResource ControlStrokeColorDefaultBrush}" />

                    <!-- Media sessions -->
                    <StackPanel x:Name="MediaSession" Visibility="{x:Bind ViewModel.Device.DeviceSettings.MediaSessionReceive, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <FlipView
                            Margin="0,8,0,0"
                            x:Name="MediaSessionFlipView"
                            x:Load="{x:Bind ViewModel.Device.RemotePlaybackSessions.Count, Converter={StaticResource InverseCountToBooleanConverter}, Mode=OneWay}"
                            Background="Transparent"
                            MaxHeight="150"
                            ItemsSource="{x:Bind ViewModel.Device.RemotePlaybackSessions, Mode=OneWay}">
                        <FlipView.ItemTemplate>
                            <DataTemplate x:DataType="models:MediaSession">
                                <Border
                                    Background="{ThemeResource CardBackgroundFillColorDefault}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefault}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="12">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Grid Grid.Row="0" ColumnSpacing="12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <!--  Thumbnail  -->
                                            <Border
                                                Grid.Column="0"
                                                Width="94"
                                                Height="94"
                                                Background="Transparent"
                                                Visibility="{x:Bind Thumbnail, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}"
                                                CornerRadius="4">
                                                <Image Source="{x:Bind Thumbnail, Mode=OneWay}" Stretch="UniformToFill" />
                                            </Border>

                                            <!--  Media Info  -->
                                            <StackPanel Grid.Column="1" Spacing="6" VerticalAlignment="Center">
                                                <TextBlock
                                                    FontSize="12"
                                                    FontWeight="SemiBold"
                                                    Text="{x:Bind AppName, Mode=OneWay}"
                                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                    TextTrimming="CharacterEllipsis"
                                                    Visibility="{x:Bind AppName, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}" />
                                                <TextBlock
                                                    FontSize="14"
                                                    FontWeight="Medium"
                                                    Text="{x:Bind TrackTitle, Mode=OneWay}"
                                                    TextTrimming="CharacterEllipsis"
                                                    TextWrapping="NoWrap"
                                                    Visibility="{x:Bind TrackTitle, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}" />
                                                <TextBlock
                                                    FontSize="12"
                                                    Text="{x:Bind Artist, Mode=OneWay}"
                                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                    TextTrimming="CharacterEllipsis"
                                                    TextWrapping="NoWrap"
                                                    Visibility="{x:Bind Artist, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}" />

                                                <!--  Playback Controls  -->
                                                <StackPanel Orientation="Horizontal" Spacing="4">
                                                    <Button
                                                        Background="Transparent"
                                                        BorderBrush="Transparent"
                                                        Click="PreviousButton_Click"
                                                        Visibility="{x:Bind CanGoPrevious, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}">
                                                            <FontIcon FontSize="14" Glyph="&#xE892;" />
                                                    </Button>
                                                    <Button
                                                        Background="Transparent"
                                                        BorderBrush="Transparent"
                                                        Click="PlayPauseButton_Click">
                                                        <FontIcon FontSize="14"  Glyph="{x:Bind IsPlaying, Converter={StaticResource BoolToPlayPauseGlyphConverter}, Mode=OneWay}" />
                                                    </Button>
                                                    <Button
                                                        Background="Transparent"
                                                        BorderBrush="Transparent"
                                                        Click="NextButton_Click"
                                                        Visibility="{x:Bind CanGoNext, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}">
                                                        <FontIcon FontSize="14" Glyph="&#xE893;" />
                                                    </Button>
                                                    <Button
                                                        Background="Transparent"
                                                        BorderBrush="Transparent"
                                                        Click="VolumeButton_Click"
                                                        Tag="{x:Bind}">
                                                        <FontIcon FontSize="14" Glyph="&#xE767;" />
                                                    </Button>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>

                                        <!--  Volume Slider  -->
                                        <Grid
                                            Grid.Row="1"
                                            Visibility="{x:Bind IsVolumeSliderVisible, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}"
                                            ColumnSpacing="8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="24" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="24" />
                                            </Grid.ColumnDefinitions>
                                            <FontIcon
                                                Grid.Column="0"
                                                Glyph="&#xE767;"
                                                FontSize="14"
                                                VerticalAlignment="Center"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                            <Slider
                                                Grid.Column="1"
                                                Minimum="0"
                                                Maximum="100"
                                                IsThumbToolTipEnabled="False"
                                                Value="{x:Bind Volume, Mode=TwoWay}"
                                                PointerCaptureLost="MediaVolumeSlider_PointerCaptureLost"
                                                Tag="{x:Bind}" />
                                            <TextBlock
                                                Grid.Column="2"
                                                FontSize="12"
                                                VerticalAlignment="Center"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                Text="{x:Bind VolumePercentText, Mode=OneWay}" />
                                        </Grid>

                                        <!--  Position Slider  -->
                                        <Grid
                                            Grid.Row="2"
                                            Visibility="{x:Bind CanSeek, Converter={StaticResource BooleanToVisibilityConverter}, Mode=OneWay}"
                                            ColumnSpacing="8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="24" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="24" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock
                                                Grid.Column="0"
                                                FontSize="12"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Left"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                Text="{x:Bind Position, Converter={StaticResource MillisecondsToTimeConverter}, Mode=OneWay}" />
                                            <Slider
                                                Grid.Column="1"
                                                Minimum="{x:Bind MinSeekTime, Mode=OneWay}"
                                                Maximum="{x:Bind MaxSeekTime, Mode=OneWay}"
                                                Value="{x:Bind Position, Mode=TwoWay}"
                                                IsThumbToolTipEnabled="False"
                                                PointerCaptureLost="MediaPositionSlider_PointerCaptureLost"
                                                Tag="{x:Bind}" />
                                            <TextBlock
                                                Grid.Column="2"
                                                VerticalAlignment="Center"
                                                FontSize="12"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                Text="{x:Bind MaxSeekTime, Converter={StaticResource MillisecondsToTimeConverter}, Mode=OneWay}" />
                                        </Grid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </FlipView.ItemTemplate>
                    </FlipView>
                    </StackPanel>
                </StackPanel>

                <!--  Notifications  -->
                <Grid Grid.Row="3" Visibility="{x:Bind ViewModel.Device, Converter={StaticResource EmptyObjectToVisibilityConverter}, Mode=OneWay}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <!--  Notifications Heading  -->
                        <TextBlock
                            Grid.Column="0"
                            FontSize="20"
                            FontWeight="SemiBold"
                            Text="{helpers:ResourceString Name=Notifications}" />

                        <!--  Clear All Button  -->
                        <Button
                            Grid.Column="1"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            Command="{x:Bind ViewModel.ClearAllNotificationsCommand}"
                            Content="{helpers:ResourceString Name=NotificationsClearButton}" />
                    </Grid>
                    <ScrollViewer
                        Grid.Row="1"
                        Margin="0,8,-18,8"
                        Padding="0,0,18,0"
                        HorizontalScrollBarVisibility="Disabled"
                        HorizontalScrollMode="Disabled"
                        VerticalScrollBarVisibility="Auto"
                        VerticalScrollMode="Enabled">
                        <ItemsRepeater ItemsSource="{x:Bind ViewModel.Notifications, Mode=OneWay}" VerticalCacheLength="4">
                            <ItemsRepeater.Layout>
                                <StackLayout Orientation="Vertical" Spacing="8" />
                            </ItemsRepeater.Layout>
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate x:DataType="models:Notification">
                                    <Border
                                        Padding="8"
                                        Background="{ThemeResource CardBackgroundFillColorDefault}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefault}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        PointerEntered="OnPointerEntered"
                                        PointerExited="OnPointerExited">
                                        <Grid RowSpacing="12" ColumnSpacing="12">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="*" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Image
                                                Grid.Row="0"
                                                Grid.Column="0"
                                                Width="48"
                                                Height="48"
                                                VerticalAlignment="Top"
                                                Source="{x:Bind Icon, Mode=OneWay}" />
                                            <StackPanel Grid.Row="0" Grid.Column="1" Spacing="4">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock
                                                        Grid.Column="0"
                                                        Foreground="{ThemeResource TextFillColorSecondary}"
                                                        FontWeight="Medium"
                                                        Text="{x:Bind AppName}" />
                                                    <StackPanel
                                                        Grid.Column="1"
                                                        HorizontalAlignment="Right"
                                                        Orientation="Horizontal"
                                                        Spacing="8">
                                                        <Button
                                                            x:Name="MoreButton"
                                                            Width="20"
                                                            Height="20"
                                                            Margin="0,0,0,0"
                                                            Padding="0"
                                                            Background="Transparent"
                                                            BorderBrush="Transparent"
                                                            IsHitTestVisible="False"
                                                            Opacity="0">
                                                            <SymbolIcon Symbol="More" />
                                                            <Button.Flyout>
                                                                <MenuFlyout Closed="MoreButtonFlyoutClosed" Placement="BottomEdgeAlignedLeft">
                                                                    <MenuFlyoutItem
                                                                        Click="OpenAppClick"
                                                                        DataContext="{x:Bind}"
                                                                        Text="{helpers:ResourceString Name=OpenApp}">
                                                                        <MenuFlyoutItem.Icon>
                                                                            <FontIcon Glyph="&#xED35;" />
                                                                        </MenuFlyoutItem.Icon>
                                                                    </MenuFlyoutItem>
                                                                    <MenuFlyoutItem
                                                                        Click="UpdateNotificationFilterClick"
                                                                        Tag="{x:Bind AppPackage}"
                                                                        Text="{x:Bind FlyoutFilterString}">
                                                                        <MenuFlyoutItem.Icon>
                                                                            <FontIcon Glyph="&#xECE4;" />
                                                                        </MenuFlyoutItem.Icon>
                                                                    </MenuFlyoutItem>
                                                                    <MenuFlyoutItem
                                                                        Click="ToggleNotificationPinClick"
                                                                        DataContext="{x:Bind}"
                                                                        Text="{x:Bind Pinned, Converter={StaticResource BoolToPinTextConverter}}">
                                                                        <MenuFlyoutItem.Icon>
                                                                            <FontIcon Glyph="{x:Bind Pinned, Converter={StaticResource BoolToPinGlyphConverter}}" />
                                                                        </MenuFlyoutItem.Icon>
                                                                    </MenuFlyoutItem>
                                                                </MenuFlyout>
                                                            </Button.Flyout>
                                                        </Button>
                                                        <Button
                                                            x:Name="CloseButton"
                                                            Width="20"
                                                            Height="20"
                                                            Margin="0,0,0,0"
                                                            Padding="0"
                                                            Command="{Binding ViewModel.RemoveNotificationCommand, ElementName=PageRoot}"
                                                            CommandParameter="{x:Bind}"
                                                            IsHitTestVisible="False"
                                                            Opacity="0"
                                                            Style="{StaticResource AlternateCloseButtonStyle}" />
                                                        <SymbolIcon
                                                            x:Name="PinIcon"
                                                            RenderTransformOrigin="0.5,0.5"
                                                            Symbol="Pin"
                                                            Tag="{x:Bind Pinned}"
                                                            Visibility="{x:Bind Pinned, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <SymbolIcon.RenderTransform>
                                                                <ScaleTransform ScaleX="0.7" ScaleY="0.7" />
                                                            </SymbolIcon.RenderTransform>
                                                        </SymbolIcon>
                                                        <TextBlock
                                                            x:Name="TimeStampTextBlock"
                                                            Grid.Column="1"
                                                            Foreground="{ThemeResource TextFillColorTertiary}"
                                                            HorizontalTextAlignment="End"
                                                            Text="{x:Bind TimeStamp, Converter={StaticResource DateTimeConverter}}"
                                                            Visibility="Visible" />
                                                    </StackPanel>
                                                </Grid>

                                                <TextBlock
                                                    x:Name="TitleTextBlock"
                                                    x:Load="{x:Bind ShouldShowTitle}"
                                                    FontWeight="SemiBold"
                                                    Text="{x:Bind Title}"
                                                    TextWrapping="Wrap" />

                                                <!--  Display Text if GroupedMessages is empty  -->
                                                <TextBlock
                                                    x:Name="FallbackTextBlock"
                                                    x:Load="{x:Bind HasGroupedMessages, Converter={StaticResource BoolNegationConverter}}"
                                                    Foreground="{ThemeResource TextFillColorTertiary}"
                                                    IsTextSelectionEnabled="True"
                                                    Text="{x:Bind Text}"
                                                    TextWrapping="Wrap" />

                                                <!--  Display GroupedMessages if not empty  -->
                                                <ItemsRepeater
                                                    x:Name="GroupedMessagesRepeater"
                                                    HorizontalAlignment="Stretch"
                                                    x:Load="{x:Bind HasGroupedMessages}"
                                                    ItemsSource="{x:Bind GroupedMessages}">
                                                    <ItemsRepeater.Layout>
                                                        <StackLayout Orientation="Vertical" Spacing="8" />
                                                    </ItemsRepeater.Layout>
                                                    <ItemsRepeater.ItemTemplate>
                                                        <DataTemplate x:DataType="models:NotificationGroup">
                                                            <StackPanel>
                                                                <TextBlock FontWeight="SemiBold" Text="{x:Bind Sender}" />
                                                                <ItemsRepeater ItemsSource="{x:Bind Messages}">
                                                                    <ItemsRepeater.Layout>
                                                                        <StackLayout Orientation="Vertical" Spacing="2" />
                                                                    </ItemsRepeater.Layout>
                                                                    <ItemsRepeater.ItemTemplate>
                                                                        <DataTemplate x:DataType="x:String">
                                                                            <TextBlock
                                                                                Foreground="{ThemeResource TextFillColorTertiary}"
                                                                                IsTextSelectionEnabled="True"
                                                                                Text="{x:Bind}"
                                                                                TextWrapping="Wrap" />
                                                                        </DataTemplate>
                                                                    </ItemsRepeater.ItemTemplate>
                                                                </ItemsRepeater>
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </ItemsRepeater.ItemTemplate>
                                                </ItemsRepeater>
                                            </StackPanel>
                                            <StackPanel
                                                x:Name="ActionsPanel"
                                                Grid.Row="1"
                                                Grid.Column="0"
                                                Grid.ColumnSpan="2"
                                                Spacing="8">

                                                <!--  Notification Reply Action  -->
                                                <Grid
                                                    x:Name="ReplyActionGrid"
                                                    ColumnSpacing="4"
                                                    Visibility="{x:Bind ReplyResultKey, Converter={StaticResource EmptyObjectToVisibilityConverter}}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>

                                                    <TextBox
                                                        x:Name="ReplyTextBox"
                                                        Grid.Column="0"
                                                        KeyDown="ReplyTextBox_KeyDown"
                                                        PlaceholderText="{helpers:ResourceString Name=Reply}"
                                                        Tag="{x:Bind}" />

                                                    <Button
                                                        x:Name="SendButton"
                                                        Grid.Column="1"
                                                        Background="Transparent"
                                                        BorderBrush="Transparent"
                                                        Click="SendButton_Click"
                                                        Tag="{x:Bind}">
                                                        <FontIcon Glyph="&#xE724;" />
                                                    </Button>
                                                </Grid>

                                                <!-- Notification Actions -->
                                                <ItemsRepeater ItemsSource="{x:Bind Actions}">
                                                    <ItemsRepeater.Layout>
                                                        <StackLayout Orientation="Horizontal" Spacing="4" />
                                                    </ItemsRepeater.Layout>

                                                    <ItemsRepeater.ItemTemplate>
                                                        <DataTemplate x:DataType="models:NotificationAction">
                                                            <Button
                                                                x:Name="ActionButton"
                                                                Background="Transparent"
                                                                BorderBrush="Transparent"
                                                                Command="{Binding ViewModel.HandleNotificationActionCommand, ElementName=PageRoot}"
                                                                CommandParameter="{x:Bind}"
                                                                Content="{x:Bind Label}" />
                                                        </DataTemplate>
                                                    </ItemsRepeater.ItemTemplate>
                                                </ItemsRepeater>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </ScrollViewer>
                </Grid>

                <!--  No Device Connected View  -->
                <Grid
                    Grid.Row="0"
                    Margin="0,20,0,0"
                    Visibility="{x:Bind ViewModel.Device, Converter={StaticResource InverseEmptyObjectToVisibilityConverter}, Mode=OneWay}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!--  Device List Section  -->
                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock
                            Grid.Row="0"
                            Margin="0,0,0,12"
                            FontSize="18"
                            FontWeight="SemiBold"
                            Text="{helpers:ResourceString Name=AvailableDevices/Title}" />

                        <!--  Available Devices  -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsRepeater ItemsSource="{x:Bind DevicesViewModel.DiscoveredDevices, Mode=OneWay}">
                                <ItemsRepeater.Layout>
                                    <StackLayout Orientation="Vertical" Spacing="8" />
                                </ItemsRepeater.Layout>
                                <ItemsRepeater.ItemTemplate>
                                    <DataTemplate x:DataType="models:DiscoveredDevice">
                                        <Button
                                            Margin="0,0,0,4"
                                            Padding="12"
                                            HorizontalAlignment="Stretch"
                                            HorizontalContentAlignment="Left"
                                            Click="DiscoveredDeviceButton_Click"
                                            Tag="{x:Bind}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <FontIcon
                                                    Grid.Column="0"
                                                    Margin="0,0,12,0"
                                                    VerticalAlignment="Center"
                                                    Glyph="&#xE8EA;" />
                                                <StackPanel Grid.Column="1" Spacing="4">
                                                    <TextBlock FontWeight="SemiBold" Text="{x:Bind Name}" />
                                                    <TextBlock
                                                        FontSize="12"
                                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                        Text="{x:Bind VerificationKey}" />
                                                </StackPanel>
                                            </Grid>
                                        </Button>
                                    </DataTemplate>
                                </ItemsRepeater.ItemTemplate>
                            </ItemsRepeater>
                        </ScrollViewer>
                    </Grid>
                </Grid>

            </Grid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <NavigationView
                    Grid.Row="0"
                    BorderBrush="Transparent"
                    IsBackButtonVisible="Collapsed"
                    IsSettingsVisible="True"
                    PaneDisplayMode="Top"
                    SelectionChanged="NavigationView_SelectionChanged">
                    <NavigationView.Resources>
                        <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="Transparent" />
                    </NavigationView.Resources>

                    <NavigationView.MenuItems>
                        <NavigationViewItem
                            Content="{helpers:ResourceString Name=Messages}"
                            IsSelected="True"
                            Tag="Messages">
                            <NavigationViewItem.Icon>
                                <FontIcon Glyph="&#xE8BD;" />
                            </NavigationViewItem.Icon>
                        </NavigationViewItem>
                        <NavigationViewItem Content="{helpers:ResourceString Name=Apps}" Tag="Apps">
                            <NavigationViewItem.Icon>
                                <FontIcon Glyph="&#xED35;" />
                            </NavigationViewItem.Icon>
                        </NavigationViewItem>
                    </NavigationView.MenuItems>

                    <NavigationView.FooterMenuItems>
                        <NavigationViewItem
                            x:Name="UpdateButton"
                            SelectsOnInvoked="False"
                            utu:CommandExtensions.Command="{x:Bind ViewModel.UpdateCommand}"
                            ToolTipService.ToolTip="Update Available"
                            Visibility="{x:Bind ViewModel.IsUpdateAvailable, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <NavigationViewItem.Icon>
                                <FontIcon Foreground="{StaticResource AccentFillColorDefaultBrush}" Glyph="&#xEDAB;" />
                            </NavigationViewItem.Icon>
                        </NavigationViewItem>
                    </NavigationView.FooterMenuItems>
                </NavigationView>

                <Frame
                    x:Name="ContentFrame"
                    Grid.Row="1"
                    Background="{ThemeResource LayerFillColorDefault}"
                    BorderBrush="{StaticResource CardStrokeColorDefault}"
                    BorderThickness="1,1,0,0"
                    CornerRadius="10,0,0,0" />
            </Grid>
        </Grid>
        <win:Grid>
            <userControls:TrayIconControl />
        </win:Grid>
    </Grid>
</Page>
