<Page
    x:Class="Sefirah.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="using:Sefirah.Helpers"
    xmlns:local="using:Sefirah.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="Transparent"
    mc:Ignorable="d">

    <NavigationView
        x:Name="SettingsNav"
        BorderThickness="0"
        IsBackButtonVisible="Collapsed"
        IsPaneOpen="True"
        IsPaneToggleButtonVisible="False"
        IsSettingsVisible="False"
        PaneDisplayMode="Left"
        SelectionChanged="NavigationView_SelectionChanged">
        <NavigationView.PaneHeader>
            <TextBlock
                x:Name="PaneHeaderText"
                Margin="24,16,0,16"
                FontSize="22"
                FontWeight="SemiBold"
                Text="{helpers:ResourceString Name=Settings}" />
        </NavigationView.PaneHeader>
        <NavigationView.Resources>
            <Thickness x:Key="NavigationViewContentGridBorderThickness">1,0,0,0</Thickness>
            <CornerRadius x:Key="NavigationViewContentGridCornerRadius">0</CornerRadius>
            <SolidColorBrush x:Key="NavigationViewContentBackground" Color="Transparent" />
            <SolidColorBrush x:Key="NavigationViewExpandedPaneBackground" Color="Transparent" />
        </NavigationView.Resources>
        <NavigationView.MenuItems>
            <NavigationViewItem
                Content="{helpers:ResourceString Name=General}"
                Icon="Setting"
                IsSelected="True"
                Tag="GeneralPage" />
            <NavigationViewItem
                Content="{helpers:ResourceString Name=Devices/Title}"
                Icon="CellPhone"
                Tag="DevicesPage" />
            <NavigationViewItem Content="{helpers:ResourceString Name=About}" Tag="AboutPage">
                <NavigationViewItem.Icon>
                    <FontIcon Glyph="&#xE946;" />
                </NavigationViewItem.Icon>
            </NavigationViewItem>
        </NavigationView.MenuItems>

        <Frame x:Name="SettingsContentFrame" Padding="40,20,0,0" />

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1280" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="SettingsNav.PaneDisplayMode" Value="Left" />
                        <Setter Target="SettingsNav.IsPaneOpen" Value="True" />
                        <Setter Target="PaneHeaderText.Visibility" Value="Visible" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="SettingsNav.IsPaneToggleButtonVisible" Value="True" />
                        <Setter Target="SettingsNav.PaneDisplayMode" Value="LeftCompact" />
                        <Setter Target="SettingsNav.IsPaneOpen" Value="False" />
                        <Setter Target="PaneHeaderText.Visibility" Value="Collapsed" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </NavigationView>
</Page>
